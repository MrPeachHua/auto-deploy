// import "/usr/local/lib/node_modules/zx/build/globals";

const app = "Project B"

echo`开始构建 ${app} ...`;
await sleep("5s");

// await $`git pull`;
// await $`npm install`;
// await $`npm run build`;
// await $`pm2 --name ${app} "npm start"`

echo`构建完成`;
